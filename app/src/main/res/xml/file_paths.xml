<?xml version="1.0" encoding="utf-8"?><!--
    Somente o cache é exposto via FileProvider: é onde ficam os áudios
    compartilhados pelo app. files-path e external-path foram removidos
    por não serem usados (evita conceder acesso além do necessário).
-->
<paths>
    <cache-path name="my_cache" path="." />
</paths>
